<Page
    x:Class="MyAlbumCoverMatchGame.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyAlbumCoverMatchGame"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:MyAlbumCoverMatchGame.Models" 
    mc:Ignorable="d">

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button Content="GetSong" Name="GetSongButton" Click="GetSongButton_Click"/>
        <ListBox Name="testListBox" ItemsSource="{x:Bind DataSource}">
            <ListBox.ItemTemplate>
                <DataTemplate x:DataType="data:Song">
                    <StackPanel Orientation="Horizontal">
                        <Image Height="40" Width="40" Source="{x:Bind Thumbnail}"></Image>
                        <TextBlock Text="{x:Bind UriStr}" Margin="10,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </StackPanel>
</Page>
